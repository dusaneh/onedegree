● To start both servers, open two separate terminals:                                                                                                         Terminal 1 - API Server (port 5000):                                        
  cd C:\pp2\od_map                                                              python src/server.py                                                        
                                                                                Terminal 2 - Web Client (port 5001):
  cd C:\pp2\od_map                                                            
  python client/src/client_server.py

  Then open your browser to http://localhost:5001 to access the web UI.

  The client UI proxies all API calls to the backend server at port 5000, so  
  both need to be running.

  Custom ports (optional):
  # API server on custom port
  python src/server.py --port 8000

  # Client on custom port (also specify API server location)
  python client/src/client_server.py --port 8001 --api-url
  http://localhost:8000


To run batch job with data in root data folder:
python src/batch_import.py --workers 8